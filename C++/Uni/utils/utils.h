#ifndef UTILS_H
#define UTILS_H

bool between( double val, double lower, double upper );
// Liefert true, wenn lower <= val <= upper ist

double sqr( double val );
// Liefert das Quadrat von val

#endif

